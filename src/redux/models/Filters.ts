export interface Filters {
    name: string,
    minAge: number,
    maxAge: number,
    minWeight: number,
    maxWeight: number,
    minHeight: number,
    maxHeight: number,
    hairColor: Array<string>,
    professions: Array<string>
}

